version: "3.3"

services:
  matrix_app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - ${APP_PORT}:8000
    command: gunicorn app.main:app --bind 0.0.0.0:${APP_PORT} --reload 
